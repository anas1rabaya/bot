# حل مشاكل البوت 🔧

## المشكلة: البوت لا يعمل

اتبع هذه الخطوات بالترتيب:

---

## الخطوة 1: التحقق من المكتبات

افتح PowerShell في مجلد المشروع وشغّل:

```bash
python -m pip install -r requirements.txt
```

انتظر حتى ينتهي التثبيت.

---

## الخطوة 2: التحقق من ملف .env

تأكد من وجود ملف `.env` في نفس المجلد وأنه يحتوي على:

```
TELEGRAM_TOKEN=8276363063:AAGoB-iSV6L8Fld1lqPxJ61YazCoHgvUeN8
GEMINI_API_KEY=your_gemini_key_here
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
```

---

## الخطوة 3: اختبار سريع

شغّل هذا الأمر:

```bash
python quick_test.py
```

**يجب أن ترى:**
- Token: 8276363063:AAGoB...
- Bot name: [اسم البوت]
- Bot username: @anas_rabaya_bot
- SUCCESS!

**إذا رأيت ERROR:**
- اكتب لي الخطأ الذي ظهر

---

## الخطوة 4: تشغيل البوت

بعد التأكد من الخطوات السابقة:

```bash
python main.py
```

**يجب أن ترى:**
- ✅ تم الاتصال بالبوت: @anas_rabaya_bot
- 🚀 جارٍ تشغيل البوت...
- 📱 افتح Telegram وابحث عن بوتك وأرسل /start

**إذا رأيت خطأ:**
- اكتب لي الخطأ الكامل الذي ظهر

---

## الأخطاء الشائعة:

### ❌ "ModuleNotFoundError: No module named 'telegram'"
**الحل:**
```bash
python -m pip install python-telegram-bot
```

### ❌ "ModuleNotFoundError: No module named 'dotenv'"
**الحل:**
```bash
python -m pip install python-dotenv
```

### ❌ "TELEGRAM_TOKEN غير موجود"
**الحل:**
- تأكد من وجود ملف `.env` في نفس المجلد
- تأكد من أن السطر يبدأ بـ `TELEGRAM_TOKEN=`

### ❌ "Unauthorized" أو "Invalid token"
**الحل:**
- التوكن غير صحيح أو منتهي
- احصل على توكن جديد من @BotFather

### ❌ البوت يعمل لكن لا يرد
**الحل:**
- تأكد من أن GEMINI_API_KEY صحيح (للردود الذكية)
- البوت يعمل بدون Gemini لكن لن يرد بشكل ذكي

---

## اختبار الاتصال بالإنترنت

```bash
python -c "from telegram import Bot; bot = Bot(token='8276363063:AAGoB-iSV6L8Fld1lqPxJ61YazCoHgvUeN8'); print(bot.get_me().username)"
```

يجب أن يطبع: `anas_rabaya_bot`

---

## إذا لم تحل المشكلة:

1. شغّل `python quick_test.py` وأرسل لي المخرجات الكاملة
2. شغّل `python main.py` وأرسل لي المخرجات الكاملة
3. تأكد من أنك في المجلد الصحيح: `C:\Users\ميثلون\Desktop\مجلد جديد`
