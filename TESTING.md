# دليل تجربة البوت محلياً 🧪

## الخطوات السريعة:

### 1. إنشاء ملف .env
أنشئ ملف `.env` في نفس المجلد وأضف:

```env
TELEGRAM_TOKEN=your_bot_token_here
GEMINI_API_KEY=your_gemini_key_here
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
```

### 2. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 3. إعداد Supabase (اختياري للتجربة)
إذا لم يكن لديك Supabase جاهز، البوت سيعمل لكن لن يحفظ المحادثات.

### 4. تشغيل البوت
```bash
python main.py
```

### 5. تجربة البوت
- افتح Telegram
- ابحث عن بوتك
- أرسل `/start`
- جرب إرسال رسالة!

---

## ملاحظات مهمة:

✅ **البوت يعمل حتى بدون Supabase** - فقط لن يحفظ المحادثات
✅ **Gemini API مطلوب** - بدونها البوت لن يرد بشكل ذكي
✅ **Telegram Token مطلوب** - بدونها البوت لن يعمل

## استكشاف الأخطاء:

### خطأ: "TELEGRAM_TOKEN غير موجود"
→ تأكد من وجود ملف `.env` وأنه يحتوي على `TELEGRAM_TOKEN`

### خطأ: "Module not found"
→ قم بتثبيت المكتبات: `pip install -r requirements.txt`

### البوت لا يرد
→ تحقق من:
- أن التوكن صحيح
- أن البوت يعمل (شاهد السجلات)
- أن Gemini API Key صحيح
