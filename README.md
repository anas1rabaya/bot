# Telegram Bot - النسخة الرقمية

بوت Telegram ذكي يستخدم Gemini API للردود الذكية ويحفظ المحادثات في Supabase.

## المميزات

- 🤖 ردود ذكية باستخدام Google Gemini API
- 💾 حفظ المحادثات في Supabase
- 👤 تمثيلك كنسخة رقمية مع معلوماتك الشخصية
- 📝 تتبع تاريخ المحادثات

## المتطلبات

- Python 3.8 أو أحدث
- حساب Telegram
- مفتاح Gemini API
- حساب Supabase

## التثبيت

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd <project-directory>
```

### 2. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 3. إعداد المتغيرات البيئية

1. أنشئ ملف `.env` في المجلد الرئيسي واملأ القيم المطلوبة:

```env
TELEGRAM_TOKEN=your_telegram_bot_token_here
GEMINI_API_KEY=your_gemini_api_key_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_key_here
```

**ملاحظة**: معلومات النسخة الرقمية موجودة مباشرة في الكود (`main.py`) ويمكن تعديلها هناك.

### 4. الحصول على Telegram Bot Token

1. افتح Telegram وابحث عن `@BotFather`
2. أرسل `/newbot` واتبع التعليمات
3. انسخ التوكن الذي يعطيك إياه BotFather
4. ضعه في ملف `.env` كقيمة لـ `TELEGRAM_TOKEN`

### 5. الحصول على Gemini API Key

1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. سجل الدخول بحساب Google
3. أنشئ مفتاح API جديد
4. انسخ المفتاح وضعه في ملف `.env` كقيمة لـ `GEMINI_API_KEY`

### 6. إعداد Supabase

1. اذهب إلى [Supabase](https://supabase.com) وأنشئ حساباً
2. أنشئ مشروعاً جديداً
3. اذهب إلى Settings > API
4. انسخ `URL` و `anon public key`
5. ضعهما في ملف `.env` كقيم لـ `SUPABASE_URL` و `SUPABASE_KEY`

### 7. إنشاء جدول المحادثات في Supabase

افتح SQL Editor في Supabase وأنشئ الجدول التالي:

```sql
CREATE TABLE conversations (
  id BIGSERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,
  user_name TEXT,
  message TEXT NOT NULL,
  response TEXT NOT NULL,
  timestamp TIMESTAMPTZ DEFAULT NOW(),
  session_id TEXT
);

CREATE INDEX idx_user_id ON conversations(user_id);
CREATE INDEX idx_timestamp ON conversations(timestamp DESC);
CREATE INDEX idx_session_id ON conversations(session_id);
```

## التشغيل

```bash
python main.py
```

## الاستخدام

1. افتح Telegram وابحث عن بوتك
2. أرسل `/start` لبدء المحادثة
3. ابدأ التحدث مع البوت!

## البنية

```
.
├── main.py              # الكود الرئيسي للبوت
├── requirements.txt     # المكتبات المطلوبة
├── .env                 # المتغيرات السرية (لا ترفعه إلى Git!)
└── README.md           # هذا الملف
```

## التخصيص

البوت مُعد مسبقاً كنسخة رقمية من **أنس ربايعة**. يمكنك تخصيص المعلومات من خلال تعديل الكود في `main.py`:

- `DIGITAL_SELF`: معلومات النسخة الرقمية (الاسم، الموقع، التعليم، الخدمات، المهارات)
- `SYSTEM_PROMPT`: الـ prompt المستخدم مع Gemini API
- `handle_special_questions()`: معالجة الأسئلة الخاصة

### معلومات النسخة الرقمية الحالية:
- **الاسم**: أنس ربايعة
- **الموقع**: فلسطين 🇵🇸
- **التعليم**: بكالوريوس GIS - الجامعة العربية الأمريكية
- **التخصص**: مبرمج AI وحلول ذكاء اصطناعي
- **الخدمات**: مواقع ويب، بوتات ذكية، حلول AI، أتمتة، تطبيقات

## الأمان

⚠️ **مهم جداً**: لا ترفع ملف `.env` إلى Git! تأكد من إضافته إلى `.gitignore`:

```
.env
```

## استكشاف الأخطاء

### البوت لا يرد

- تأكد من أن `TELEGRAM_TOKEN` صحيح
- تأكد من أن البوت يعمل (تحقق من السجلات)

### الأخطاء من Gemini API

- تأكد من أن `GEMINI_API_KEY` صحيح
- تحقق من أن لديك رصيد كافٍ في حساب Google AI

### مشاكل Supabase

- تأكد من أن الجدول `conversations` موجود
- تحقق من أن `SUPABASE_URL` و `SUPABASE_KEY` صحيحة
- تأكد من أن الصلاحيات (permissions) صحيحة في Supabase

## الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه بحرية.

## المساهمة

نرحب بمساهماتك! يرجى فتح issue أو pull request.
