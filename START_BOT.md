# كيفية تشغيل البوت 🚀

## الخطوات السريعة:

### 1. تأكد من وجود ملف .env
يجب أن يحتوي على:
```
TELEGRAM_TOKEN=8276363063:AAGRfEZBn76lBiCWppyrbcpEtHUit5SZgeA
GEMINI_API_KEY=your_gemini_key_here
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
```

### 2. تثبيت المكتبات (إذا لم تكن مثبتة)
```bash
python -m pip install -r requirements.txt
```

### 3. تشغيل البوت
```bash
python main.py
```

### 4. تجربة البوت
- افتح Telegram
- ابحث عن بوتك
- أرسل `/start`

---

## استكشاف المشاكل:

### ❌ "TELEGRAM_TOKEN غير موجود"
→ تأكد من وجود ملف `.env` في نفس المجلد

### ❌ "التوكن غير صحيح"
→ تحقق من التوكن في BotFather

### ❌ "Module not found"
→ قم بتثبيت المكتبات: `python -m pip install -r requirements.txt`

### ⚠️ البوت يعمل لكن لا يرد
→ تحقق من:
- أن GEMINI_API_KEY صحيح (للردود الذكية)
- أن Supabase صحيح (لحفظ المحادثات)

---

## ملاحظة:
البوت يجب أن يعمل في نافذة Terminal/PowerShell مفتوحة.
لا تغلق النافذة أثناء استخدام البوت!
